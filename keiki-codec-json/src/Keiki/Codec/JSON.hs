-- |
-- Module      : Keiki.Codec.JSON
-- Description : JSON codec for keiki's @RegFile rs@ (M0 scaffold).
--
-- This module is the public entry point of the @keiki-codec-json@
-- package. M0 ships an empty scaffold so the cabal project compiles;
-- M2 lands the @RegFileToJSON@ class and inductive instances per
-- @docs\/plans\/36-regfile-json-codec-and-shape-hash-for-snapshot-persistence.md@.
module Keiki.Codec.JSON () where
