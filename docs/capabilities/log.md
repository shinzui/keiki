# Bundle Update Log

## 2026-08-08
* **Adoption**: Authored the capability catalog against the shared
  `coordination.capabilities` profile (okf-profiles v0.9.0). Twelve capabilities
  (CAP-1 … CAP-12) derived from source, tests, README/ROADMAP, and the three
  packages' changelogs; every record backed by openable evidence. Registered the
  `capabilities` bundle in `mori.dhall`.
